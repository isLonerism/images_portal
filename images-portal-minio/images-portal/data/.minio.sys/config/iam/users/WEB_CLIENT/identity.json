{
    "version": 1,
    "credentials": {
        "accessKey": "${WEB_CLIENT_S3_ACCESS_KEY}",
        "secretKey": "${WEB_CLIENT_S3_SECRET_KEY}",
        "expiration": "0001-01-01T00:00:00Z",
        "status": "enabled"
    }
}