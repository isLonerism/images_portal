window.ENV = {
    "S3_BUCKET_NAME": "${S3_BUCKET_NAME}",
    "S3_ACCESS_KEY_ID": "${S3_ACCESS_KEY_ID}",
    "S3_SECRET_ACCESS_KEY": "${S3_SECRET_ACCESS_KEY}",
    "S3_ENDPOINT": "${S3_ENDPOINT}",
    "S3_UPLOAD_TIMEOUT_MS": "${S3_UPLOAD_TIMEOUT_MS}",
    "IMAGE_PUSH_ROUTE": "${IMAGE_PUSH_ROUTE}",
    "IMAGE_LOAD_ROUTE": "${IMAGE_LOAD_ROUTE}",
    "OSFT_REGISTRY_PREFIX": "${OSFT_REGISTRY_PREFIX}",
    "LOAD_TIMEOUT_MS": "${LOAD_TIMEOUT_MS}",
    "PUSH_TIMEOUT_MS": "${PUSH_TIMEOUT_MS}",
    "OPENSHIFT_API_ENDPOINT": "${OPENSHIFT_API_ENDPOINT}",
    "OPENSHIFT_OAUTH_ENDPOINT": "${OPENSHIFT_OAUTH_ENDPOINT}",
    "OAUTH_CLIENT_ID": "${OAUTH_CLIENT_ID}"
}
